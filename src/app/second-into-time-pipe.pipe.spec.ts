import { SecondIntoTimePipePipe } from './second-into-time-pipe.pipe';

describe('SecondIntoTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondIntoTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
